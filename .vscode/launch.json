{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "(lldb) Launch",
            "type": "lldb",
            "request": "launch",
            "program": "${workspaceFolder}/test/a.out",
            "args": [
                "<", 
                "input"
            ],
            "cwd": "${workspaceFolder}/test/",
            //"preLaunchTask": "build"
        }
    ]
}
